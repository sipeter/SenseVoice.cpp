# 阶段 5C：PC 到 C++ 音频密钥登记

日期：2026-07-19
状态：实施中（C++ 注册表已完成，PC 下发待接入）

## 1. 步骤 5C-1：C++ 本机会话注册表

### 1.1 协议边界

PC 与 `ZianCore.exe` 继续使用父进程创建的重定向标准输入管道，不增加网络管理端口，也不把密钥放入进程命令行。登记指令固定为：

```text
LINK_V2_AUDIO_REGISTER <sessionIdHex32> <audioKeyHex64> <noncePrefixHex8>
LINK_V2_AUDIO_REMOVE <sessionIdHex32>
LINK_V2_AUDIO_CLEAR
```

- `sessionId` 是 RFC 4122 网络顺序的 16 字节 UUID，以 32 位十六进制传递。
- `audioKey` 固定 32 字节，nonce prefix 固定 4 字节。
- 字段数量、长度或十六进制字符不合法时拒绝整条指令，不改变已有登记。
- stdout 只返回不含标识和秘密的成功/拒绝状态；日志不输出密钥、nonce prefix 或完整登记命令。

### 1.2 实现

- 新增线程安全 `link_v2_audio_session_registry`，支持登记、覆盖、查找、撤销和全部清理。
- 覆盖、撤销、析构和 clear 时 best-effort 覆盖内存中的会话 ID、音频密钥和 nonce prefix。
- socket-only、dual-audio 及麦克风不可用后的 socket 降级路径均在原有 START/STOP 解析前处理登记命令。
- 原有 Link v1 START/STOP 和裸 PCM 接收行为保持不变；本步骤尚未启用 ZL2A 解密。

### 1.3 验证

- 新增 Release 配置仍有效的独立测试（不依赖 `assert`），覆盖合法登记与查找、非法登记不覆盖旧密钥、撤销、未知旧命令透传及畸形 clear 拒绝。
- 使用新的独立 CMake 构建目录配置 Visual Studio 2022 x64，避免使用记录旧路径的既有 `build` 缓存。
- `link_v2_audio_session_tests` 构建并通过 CTest。
- `ZianCore` Release x64 完整构建成功。
- `git diff --check` 通过。
- 验证结束后仅删除本步骤生成的三个临时构建目录；这些目录是可重新生成的 CMake 产物，不可恢复但不包含源代码或用户数据。

### 1.4 提交

- C++ 注册表代码提交：`d877f1e`。
- 本步骤首次过程文档提交：`55bb38e`。

## 2. 下一步

- PC 从已认证的 Link v2 会话安全复制 `audioKey` 与 `audioNoncePrefix`。
- 每次手机录音会话在 `START PHONE` 前先登记其 recording `sessionId`，收到 `[[STOPPED]]` 后撤销。
- 随后让 C++ TCP 8182 根据 `ZL2A` 头部查找登记并执行防重放与 AES-GCM 解密。
